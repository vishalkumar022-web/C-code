#include<stdio.h>
int main(){

int a , b ; 
printf ("enter the value of a , b ");
scanf ("%d%d",&a,&b);

 (a>=b) ? (a>b) ? printf("a is max %d",a) : printf("a==b") :  printf("b is max= %d",b) ;
 

    return 0 ;
}